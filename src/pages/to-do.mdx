import {Typography} from "@material-ui/core";

export default ({children}) => <Typography>{children}</Typography>;

# Stuff to do

- [ ] Remove material-ui. This was cool, but let's just raw css.
- [ ] Use another mdx engine - ther must be a few
- [ ] Create the dnd viewer. That'll be fun
- [ ] Json viewer and editor.
