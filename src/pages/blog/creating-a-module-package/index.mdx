import BlogPost from '../../../components/BlogPost'

export const meta = {
    title: 'creating a module package',
    description: 'creating a module package',
    date: '20210704',
    readTime: '5',
	draft: true
}

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

## I have created a repo with an app inside it.

I wish to use the app from elsewhere
