import BlogPost from '../../../components/BlogPost'
import LandMining from "../../../components/LandMining";

export const meta = {
    title: 'Land Mines',
    description: 'Find all the land mines',
    date: '20210914',
    readTime: '5',
}

export default ({ children }) => <LandMining/>;

export const nav = (typeof navigator === 'undefined') ? "no navigator" : navigator


