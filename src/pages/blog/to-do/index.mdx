import BlogPost from '../../../components/BlogPost'
import {DemoButton} from '@andrew.oxenburgh/cool-button-demo-imported-react-button'

export const meta = {
    title: 'To do',
    description: 'To do list',
    date: 'August 2, 2021',
    readTime: '5',
}

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

## Stuff to do
- [ ] Remove material-ui. This was cool, but let's use just raw css.
- [ ] Use another mdx engine - ther must be a few
- [ ] Create the dnd viewer. That'll be fun
- [ ] Json viewer and editor.
